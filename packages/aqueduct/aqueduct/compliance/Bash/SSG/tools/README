- measure_lockdown gives the current status of automated bash remediation.
	-- takes in no arguments 
	-- tells the percentage of automated dcid_lockdown remediation   

- fix_mapper is to be run before making the scap-security-guide project.  it
  creates a fix file in the scap-security-guide inputs.
	-- takes in no arguments 
	-- creates a fix file, which is transformed in ssg to produce xccdf
		with dcid_lockdown fix tags

- dcid_6-3.xml contains a ssg profile with all remediation that is currently
  mapped to dcid references (with the actual references removed)

- dcid_6-3_manual.xml contains an ssg profile that contains dcid referenced
  remediation that requires user input
